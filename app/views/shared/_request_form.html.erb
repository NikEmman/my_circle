<%= form_with(model: follow_request, url: profile_follow_requests_path(profile), local: true) do |f| %>
  <%= f.submit "Follow", class:"bg-transparent text-white font-semibold hover:text-white py-2 px-4 border border-white rounded-full hover:border-transparent hover:bg-blue-500"%>
<% end %>
