<%= form_with(model: @follow_request, url: profile_follow_requests_path(@profile), local: true) do |f| %>
  <%= f.submit "Follow", class:"bg-blue-400 w-48 mt-5 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full"%>
<% end %>
