<%= form_with(model: @follow_request, url: profile_follow_requests_path(@profile), local: true) do |f| %>
  <%= f.submit "Follow", class:"rounded-xl bg-slate-600 p-2 text-teal-400 hover:bg-slate-400 hover:text-teal-700"%>
<% end %>
