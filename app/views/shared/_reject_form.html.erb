<%= form_with(model: request, url: profile_follow_request_path(@profile, request), local: true) do |f| %>
  <%= f.hidden_field :status, value: 2 %>
  <%= f.submit "Reject", class:"bg-red-400 w-24 mt-5 hover:bg-red-600 text-white font-bold py-2 px-2 rounded-full"%>
<% end %>
