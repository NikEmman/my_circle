<%= form_with(model: request, url: profile_follow_request_path(@profile,request), local: true) do |f| %>
  <%= f.hidden_field :status, value: 1 %>
  <%= f.submit "✓",class:"hover:bg-green-600 bg-transparent text-white font-semibold hover:text-white size-8 p-1 border border-white rounded-full hover:border-transparent " %>
<% end %>
