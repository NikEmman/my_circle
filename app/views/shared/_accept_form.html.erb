<%= form_with(model: request, url: profile_follow_request_path(@profile,request), local: true) do |f| %>
  <%= f.hidden_field :status, value: 1 %>
  <%= f.submit "Accept",class:"hover:bg-green-600 bg-transparent text-white font-semibold hover:text-white py-2 px-4 border border-white rounded-full hover:border-transparent " %>
<% end %>
